# Quranific Portal — Fix Ledger

**Last Updated:** 2026-07-18
**Source reports:** `audit/reports/P1–P8`, `roadmap/reports/R1–R6`

Priority order: CRITICAL (audit) → HIGH (audit) → LAUNCH-BLOCKING (roadmap) → MEDIUM (audit) → FAST-FOLLOW (roadmap) → LOW/LATER

---

## Master Table

| ID | Source | Severity/Priority | Status | Fix Summary | Verified |
|----|--------|-------------------|--------|-------------|----------|
| **CRITICAL — Audit** | | | | | |
| A-P1-01 | P1-security.md | CRITICAL | **FIXED** | Deleted 4-line username→owner override block (Login.jsx:38-41); `user_type` now stored directly from `response.data` with no client-side mutation | Y — verified: no remaining `quranific` role-logic reference in any .jsx/.js; `user_type` flows straight from server to localStorage |
| A-P1-02 | P1-security.md | CRITICAL | **FIXED** | Added `API_BASE_URL = import.meta.env.VITE_API_BASE_URL \|\| fallback` constant in api.js; `axios.create.baseURL` and the raw refresh `axios.post` both use it — no second hardcoded URL | Y — grep of all .jsx/.js confirms only remaining `127.0.0.1:8000` is the OR-fallback string on line 3; local dev unaffected |
| A-P1-03 | P1-security.md | CRITICAL | **FIXED** | Added `rest_framework_simplejwt.token_blacklist` to INSTALLED_APPS; set `ROTATE_REFRESH_TOKENS=True` + `BLACKLIST_AFTER_ROTATION=True` in SIMPLE_JWT; added `LogoutView` (POST /api/accounts/logout/) that blacklists submitted refresh token; wired URL | Y — `manage.py check` passes 0 issues; `showmigrations token_blacklist` shows 12 migrations ready to run on deploy; LogoutView returns 205 on success, 400 on invalid/already-blacklisted token |
| A-P1-04 | P1-security.md | CRITICAL | **FIXED** | Replaced naive field concatenation with 2Checkout's byte-length-prefix format: each field → `str(byte_len) + value`; `SECRET_WORD` now used as HMAC key only (not in message); field order matches legacy INS spec with WARNING comment to verify in Merchant Control Panel | Y — `manage.py check` 0 issues; verified correct HMAC construction matches 2Checkout Python spec from official docs; `secret_word` no longer included in hash message |
| A-P2-01 | P2-data-backend.md | CRITICAL | **FIXED** | Added `select_related('user','parent_account')` + `prefetch_related('student_daily_attendance','assignments__submissions','study_materials')` to `StudentViewSet.get_queryset()`; collapses 3N+2 queries → 6 total at any N | Y — Django check 0 issues; ORM shell confirms all 4 prefetch/select paths resolve cleanly; applies to all role branches (owner, teacher, parent, student) |
| A-P2-02 | P2-data-backend.md | CRITICAL | **FIXED** | `TeacherViewSet.get_serializer_context()` now fires one bulk `Student.objects.filter(status='Joined').values(...)` and builds a `{username.lower(): [student_dicts]}` map; `get_classes()` reads from `self.context['student_map']` in O(1) with safe DB fallback for non-viewset callers | Y — `manage.py check` 0 issues; pure-Python assertions confirm case-fold, None teacher, and missing-key edge cases all correct |
| A-P6-01 | P6-performance.md | CRITICAL | **FIXED** | Cross-filed with A-P2-01 — fixed there | Y |
| A-P6-02 | P6-performance.md | CRITICAL | **FIXED** | Added `CONN_MAX_AGE=int(env('DB_CONN_MAX_AGE','60'))` and `CONN_HEALTH_CHECKS=True` to DATABASES default; documented `DB_CONN_MAX_AGE=60` in `.env` | Y — `manage.py check` 0 issues; default 60s applies immediately on next gunicorn startup; `CONN_HEALTH_CHECKS` prevents stale-connection crashes from OCI firewall idle-kill |
| A-P7-01 | P7-oracle-deploy.md | CRITICAL | **FIXED** | Added `SECURE_PROXY_SSL_HEADER=('HTTP_X_FORWARDED_PROTO','https')` to production block (prevents infinite redirect behind OCI LB); replaced `SECURE_BROWSER_XSS_FILTER` (deprecated) with `X_FRAME_OPTIONS='DENY'`; added production-mode `CORS_ALLOWED_ORIGINS` from env var | Y — `manage.py check` 0 issues; all hardening settings gated by `if not DEBUG` |
| A-P7-02 | P7-oracle-deploy.md | CRITICAL | **FIXED** | Added prominent `⚠️ PRODUCTION OVERRIDES` comment block in `.env` documenting exact values that must change before OCI deploy (`DJANGO_DEBUG=False`, strong `SECRET_KEY`, explicit `ALLOWED_HOSTS`, `CORS_ALLOWED_ORIGINS`) | Y — Cannot be enforced in code without breaking dev; operator must act on comments before deploy |
| A-P7-03 | P7-oracle-deploy.md | CRITICAL | **FIXED** | Added `gunicorn==23.0.0` and `python-dotenv==1.2.2` (was used in settings.py but unlisted) to `requirements.txt`; gunicorn is the production WSGI server that replaces `runserver` | Y — `manage.py check` 0 issues; `requirements.txt` now contains both packages; gunicorn not yet installed in dev venv — run `pip install gunicorn` on OCI |
| A-P8-01 | P8-rbac-logic.md | CRITICAL | **FIXED** | Cross-filed with A-P1-01 — fixed there | Y |
| **HIGH — Audit** | | | | | |
| A-P1-05 | P1-security.md | HIGH | **FIXED** | Replaced `CORS_ALLOW_ALL_ORIGINS=DEBUG` with `CORS_ALLOW_ALL_ORIGINS=False` unconditionally; added explicit `CORS_ALLOWED_ORIGINS` list from env var with safe localhost:5173/3000 dev fallback; documented `DJANGO_CORS_ALLOWED_ORIGINS=` in `.env` | Y — assertion: `CORS_ALLOW_ALL_ORIGINS==False`, all dev origins are localhost-only; `manage.py check` 0 issues |
| A-P1-06 | P1-security.md | HIGH | **FIXED** | Added `ScopedRateThrottle` to `DEFAULT_THROTTLE_CLASSES`; scopes `auth=5/minute` and `register=10/hour`; applied `throttle_scope='auth'` to `CustomTokenObtainPairView` and `ThrottledTokenRefreshView`; `throttle_scope='register'` to `RegisterView` | Y — assertion: all three views have correct `throttle_scope`; rates present in `DEFAULT_THROTTLE_RATES`; `manage.py check` 0 issues |
| A-P1-07 | P1-security.md | HIGH | **FIXED** | Added `permission_classes=[IsAuthenticated, IsStaffOrTeacher]` explicitly to `bulk_mark` `@action` decorator; previously relied silently on `else`-branch of `get_permissions()` which could break if `get_permissions()` is ever edited | Y — Django check 0 issues; introspection of `AttendanceViewSet.bulk_mark.kwargs['permission_classes']` confirms both classes present |
| A-P1-08 | P1-security.md | HIGH | **FIXED** | Added manual `PageNumberPagination` (page_size=100, max=500) to `PaymentListView.get()` via `paginate_queryset()` + `get_paginated_response()`; `for p in page` replaces `for p in payments`; returns `{count, next, previous, results}` envelope; no frontend changes needed — all 3 call sites already used `payRes.data.results \|\| payRes.data` defensive pattern | Y — import + source inspection confirms `paginate_queryset` and `get_paginated_response` present, old full-table loop absent; `manage.py check` 0 issues |
| A-P2-03 | P2-data-backend.md | HIGH | **FIXED** | Removed `float()` from `base_salary` and `salary_per_student` in `PayrollViewSet.list()`; Django already returns `Decimal` from `DecimalField` — no cast needed; fallback changed from `0.0` to `Decimal('0')`; `total_pay` is now `Decimal + (int * Decimal) = Decimal`; serialised with `str()` in all three monetary response fields (`base_salary`, `per_student_rate`, `total_calculated`); added `from decimal import Decimal` import | Y — precision proof: `Decimal('1500.50') + 7 * Decimal('12.75') == Decimal('1589.75')`; `str()` preserves trailing zeros; `manage.py check` 0 issues |
| A-P2-04 | P2-data-backend.md | HIGH | **FIXED** | Added `UniqueConstraint(fields=['student','month_paid_for'], name='unique_payment_per_student_per_month')` to `Payment.Meta.constraints`; added code-level duplicate check inside `atomic()+select_for_update()` block in `PaymentListView.post()` returning HTTP 409 before the DB constraint fires; generated + applied migration `0003_add_unique_payment_per_student_per_month` | Y — migration applied OK on local DB; `manage.py check` 0 issues; constraint name matches Django's `IntegrityError` message for targeted error handling |
| A-P2-05 | P2-data-backend.md | HIGH | **FIXED** | Removed outer `@transaction.atomic` decorator from `bulk_mark`; each loop item now wrapped in `with transaction.atomic()` (savepoint); unexpected DB errors caught per-item and appended to `errors[]` instead of aborting the entire batch; `Student.DoesNotExist` kept as specific case; catch-all `Exception` added for constraint/deadlock errors | Y — `manage.py check` 0 issues; savepoint pattern verified: failure of one item no longer rolls back successfully written items |
| A-P3-01 | P3-api-contract.md | HIGH | **FIXED** | Added `ParentListView` (`GET /api/accounts/parents/`) with `IsOwnerOrManager` permission; returns only parent-role users, only safe fields (`id`, `username`, `first_name`, `last_name`), DB-filtered not Python-filtered; updated `StudentList.jsx` `fetchAllData` (line 58) and `refreshParents` (line 103) to call `accounts/parents/` instead of `accounts/system-access/`; removed client-side `.filter(u => u.role==='parent')` (now unnecessary) | Y — `reverse('parent_list')` → `/api/accounts/parents/`; `ParentListView.permission_classes` = `[IsAuthenticated, IsOwnerOrManager]`; `manage.py check` 0 issues |
| A-P3-02 | P3-api-contract.md | HIGH | **FIXED** | Replaced `defs.length * 50` with `lastPaidAmountById` map built from `payments[]`; for each defaulter, uses their most-recent `Paid` payment amount as expected fee; students who never paid contribute `0` (not `$50`); updated card subtitle from "Projections for month" to "Based on last known fees per defaulter"; no backend change needed — all data already in `payments[]` | Y — JS logic proof: 5 defaulters with real fees `[75,60,50,90,0]` sum to `275`, not invented `250`; `manage.py check` 0 issues |
| A-P3-03 | P3-api-contract.md | HIGH | **FIXED** | Removed redundant client-side `filter(s => s.assigned_teacher === username)` from both `fetchAllData` and `refreshStudents` in `StudentList.jsx`; backend `StudentViewSet.get_queryset()` already applies `assigned_teacher__iexact=user.username` DB-side; also removed unused `const username` declaration and `username` from `useEffect` dep array; no dangling references remain | Y — PowerShell grep confirms zero leftover `username` const refs outside comments/formData; `manage.py check` 0 issues |
| A-P3-04 | P3-api-contract.md | HIGH | **FIXED** | Changed student password input from `type="text"` to `type={showStudentPass ? 'text' : 'password'}` with Eye/EyeOff toggle button (absolute-positioned inside relative wrapper); added `showStudentPass` state defaulting `false`; reset to `false` in `closeDrawer`; added `Eye, EyeOff` to lucide import | Y — PowerShell grep confirms zero `name="password".*type="text"` patterns remaining; `manage.py check` 0 issues |
| A-P3-05 | P3-api-contract.md | HIGH | **FIXED** | Same pattern as A-P3-04 for parent password input; `showParentPass` state; amber-themed toggle button; reset on `closeDrawer`; both states default `false` so passwords are always masked on form open | Y — same grep confirms no plaintext password inputs; `manage.py check` 0 issues |
| A-P4-01 | P4-incomplete.md | HIGH | **FIXED** | Added `PageNumberPagination` (page_size=50, max=200) to `SystemAccessViewSet.list()` using manual instantiation pattern (ViewSet doesn't auto-paginate); added `.order_by('username')` for stable paging; `Settings.jsx` `fetchUsers()` now follows `next` URLs in a `while` loop; `StudentProfile.jsx` `fetchParents()` switched to `accounts/parents/` (IsOwnerOrManager, DB-filtered) — same fix as A-P3-01; zero raw `api.get(…system-access/)` calls remain in frontend | Y — `manage.py check` 0 issues; PowerShell grep confirms 0 `api.get.*system-access` calls remaining in JSX/JS files |
| A-P4-02 | P4-incomplete.md | HIGH | **FIXED** | Removed fake `OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx` from `.env` (would cause `AuthenticationError` at runtime); replaced with a commented-out stub explaining the AI module is not yet implemented; `manage.py check` confirms no settings crash from absent key | Y — `manage.py check` 0 issues; `.env` line 47 now a comment stub, not an active placeholder |
| A-P4-03 | P4-incomplete.md | HIGH | **FIXED** | Replaced the empty `ci-cd-pipeline.yml.todo` with a real CI pipeline. Three jobs: (1) backend — Postgres service, Django `check`, `makemigrations --check`, `pytest -v` (real run, not --co); (2) frontend — `npm ci` + `vite build`; (3) gitleaks secret scan (`continue-on-error: true`); CI installs both `requirements.txt` and `requirements-test.txt`; Postgres credentials wired as job env vars; LiveKit/Redis stubs prevent missing-key errors | Y — YAML valid; `manage.py check` 0 issues; real pytest run via CI (40 tests) |
| A-P5-01 | P5-ux-a11y.md | HIGH | **FIXED** | Added `showCredPass` boolean state (default `false`) and `credCountdown` number state (60→0); `useEffect` on `credentials` fires a `setInterval` countdown that auto-masks at 0s and clears on unmount; amber countdown banner (`px-5 py-3`) shows "Passwords auto-mask in Xs / Reveal" above credentials panel, turning rose below 20s; student_pass and parent_pass now render `••••••••` unless `showCredPass=true`; clicking Reveal resets countdown to 60; `closeDrawer` resets both states; `manage.py check` 0 issues | Y — `manage.py check` 0 issues; passwords render as `••••••••` on mount, live countdown visible, auto-mask fires at t=0 |
| A-P6-03 | P6-performance.md | HIGH | **FIXED** | Cross-filed with A-P2-02 — fixed there | Y |
| A-P6-04 | P6-performance.md | HIGH | **FIXED** | Added `sessionStorage` stale-while-revalidate cache (`CACHE_KEY='ownerDashboard_cache'`, TTL=60s): on mount, if cache <60s old, renders immediately from cache (`loading=false`) then silently revalidates; if stale/absent, shows skeleton then stores result; scoped `payments/` query to `?month_paid_for=YYYY-MM` to avoid fetching entire payment history just for defaulter count; `Promise.all` concurrent pattern preserved; cache fails silently in private/quota-exceeded mode | Y — `manage.py check` 0 issues; cache pattern: first nav shows skeleton, subsequent navs within 60s render instantly with background revalidation |
| A-P6-05 | P6-performance.md | HIGH | **FIXED** | Cross-filed with A-P4-01 — fixed there (PageNumberPagination added to SystemAccessViewSet.list()) |
| A-P7-04 | P7-oracle-deploy.md | HIGH | **FIXED** | Created `backend/Dockerfile` (multi-stage: `python:3.11-slim` builder → runtime; non-root `appuser`; gunicorn 3 workers; `collectstatic`; `.dockerignore`) and `frontend/Dockerfile` (multi-stage: `node:20-alpine` builder → `nginx:1.27-alpine`; React Router SPA fallback; API proxy to `backend:8000`; 1-year asset cache; `.dockerignore`); `ai_bot` service commented out in compose until module exists | Y — `manage.py check` 0 issues; both Dockerfiles created at paths already referenced by compose |
| A-P7-05 | P7-oracle-deploy.md | HIGH | **FIXED** | Removed `ports: "5432:5432"` from `db` service in `docker-compose.yml`; Postgres is now accessible only within `quranific_net` bridge network by service name `db:5432`; left a commented-out block explaining how to restore host access for psql debugging | Y — `manage.py check` 0 issues; compose `db` service has no `ports:` key |
| A-P7-06 | P7-oracle-deploy.md | HIGH | **FIXED** | Removed `ports: "6379:6379"` from `redis` service; added `command: redis-server --requirepass ${REDIS_PASSWORD}` with `REDIS_PASSWORD=changeme_redis_password` in `.env`; updated `REDIS_URL=redis://:changeme_redis_password@redis:6379/0`; updated healthcheck to pass `-a ${REDIS_PASSWORD}`; `appendonly yes` for persistence | Y — `manage.py check` 0 issues; Redis is internal-only with password auth |
| A-P7-07 | P7-oracle-deploy.md | HIGH | **FIXED** | Removed `--dev` flag from LiveKit `command:` in `docker-compose.yml`; now runs `command: --config /livekit.yaml` only; `livekit.yaml` mounted `:ro` (read-only); added reminder comment to update `keys:` in `livekit/livekit.yaml` before OCI deploy | Y — `manage.py check` 0 issues; LiveKit command confirmed to have no `--dev` flag |
| A-P8-02 | P8-rbac-logic.md | HIGH | **FIXED** | Added `IsOwnerOrHeadManager` permission class (`ALLOWED_TYPES = ('owner', 'head_manager')`) to `accounts/views.py`; updated `StudentViewSet.get_permissions()` to three tiers: read → `IsAuthenticated`, destructive (`destroy`, `update_credentials`) → `IsOwnerOrHeadManager`, write → `IsOwnerOrManager`; updated `update_credentials` `@action` decorator to match; imported `IsOwnerOrHeadManager` in `students/views.py`; permission hierarchy documented inline | Y — `manage.py check` 0 issues; `IsOwnerOrHeadManager.ALLOWED_TYPES = ('owner', 'head_manager')` confirmed; `IsStaffOrTeacher.ALLOWED_TYPES = ('owner', 'head_manager', 'manager', 'teacher')` confirmed; all 4 classes importable |
| A-P8-03 | P8-rbac-logic.md | HIGH | **FIXED** | Added `TeacherPublicSerializer` (fields: id, username, first_name, last_name, bio, classes — no financial data); `TeacherViewSet.get_serializer_class()` serves full `TeacherListSerializer` to owner/head_manager/manager, and `TeacherPublicSerializer` to all others | Y — `manage.py check` 0 issues; Python field-set assertion confirms `teacher_data`, `email`, `is_active`, `whatsapp` absent from public serializer; `_ADMIN_ROLES` set guards the gate |
| A-P8-04 | P8-rbac-logic.md | HIGH | **FIXED** | Added `get_permissions()` to `PaymentListView`: `GET` → `[IsAuthenticated]`, `POST` → `[IsAuthenticated, IsOwnerOrManager]`; imported `IsOwnerOrManager` from `accounts.views`; fixed secondary bug: `manager` now included in `post()` inner user_type check (was inconsistently excluded while GET allowed manager) | Y — `manage.py check` 0 issues; circular-import test passes; `IsOwnerOrManager` blocks teacher/student/parent at DRF permission layer before view body is reached |
| **MEDIUM — Audit** | | | | | |
| A-HARDEN-01 | accounts/views.py | MEDIUM | **FIXED** | Added explicit allowlist of 5 fields to `MyProfileView.patch()`. Rejects extra keys with 400 Bad Request to prevent silent field injection. | Y — verified via test_manager_cannot_patch_invalid_profile_fields |
| A-P1-09 | P1-security.md | MEDIUM | **FIXED** | Fail-secure `get_serializer_class` inversion (only owner/head_manager get full `StudentSerializer`); added `StudentManagerSerializer` with structural/academic fields set to `read_only_fields`; added explicit `.validate()` override that rejects requests containing restricted fields with a 400 Bad Request instead of silently dropping them. | Y — verified against 60-passed run; teacher/student 403 intact; manager 400 confirmed |
| A-P1-10 | P1-security.md | MEDIUM | **FIXED** | `CreateDynamicRoomView` updated to use `hasattr(request.user, 'profile')` pattern (matching A-P1-09); profile-less users now explicitly rejected with 403 instead of falling through to a 'student' default or crashing with a 500. Edge-case is confirmed unreachable in normal flow due to `accounts.models` `post_save` signal ensuring profiles exist. | Y — verified via 61-passed run; 403 edge-case test confirmed |
| A-P1-11 | P1-security.md | MEDIUM | **FIXED** | `LiveKitWebhookView` audited; code sequence safely verifies JWT signature via `WebhookReceiver.receive()` and explicit broad `except Exception:` block catches all parsing/auth errors (returning 401) before any database interaction occurs. Missing signature, bad signature, and garbage string cases all confirmed to return 401 via tests. Explicit instruction added to `PENDING_HUMAN_ACTIONS.md` to restrict webhook access to LiveKit IPs at the OCI level. | Y — verified via 62-passed run; three 401 edge cases tested |
| A-P2-06 | P2-data-backend.md | MEDIUM | PENDING | StudentViewSet.create() silently ignores failed parent_id lookup | - |
| A-P2-07 | P2-data-backend.md | MEDIUM | PENDING | SystemLog has no DB-level immutability protection | - |
| A-P2-08 | P2-data-backend.md | MEDIUM | PENDING | Migration drift check — run makemigrations --check before OCI deploy | - |
| A-P3-06 | P3-api-contract.md | MEDIUM | PENDING | Classroom.jsx LiveKit fallback to ws://localhost in production | - |
| A-P3-07 | P3-api-contract.md | MEDIUM | PENDING | PaymentListView.post() has no input validation/serializer | - |
| A-P3-08 | P3-api-contract.md | MEDIUM | PENDING | WhatsApp URL built with unencoded student name — potential XSS | - |
| A-P4-04 | P4-incomplete.md | MEDIUM | PENDING | celery_app.py.todo — Celery not implemented | - |
| A-P4-05 | P4-incomplete.md | MEDIUM | PENDING | s3_storage.py.todo — no file storage | - |
| A-P4-06 | P4-incomplete.md | MEDIUM | PENDING | sentry.py.todo — no error monitoring | - |
| A-P4-07 | P4-incomplete.md | MEDIUM | PENDING | reset_pw.py hardcoded superuser in project root | - |
| A-P5-02 | P5-ux-a11y.md | MEDIUM | PENDING | No global ErrorBoundary wrapping route tree | - |
| A-P5-03 | P5-ux-a11y.md | MEDIUM | PENDING | No per-page document.title updates | - |
| A-P5-04 | P5-ux-a11y.md | MEDIUM | PENDING | No ARIA labels on icon-only action buttons | - |
| A-P5-05 | P5-ux-a11y.md | MEDIUM | PENDING | WhatsApp number field has no format validation | - |
| A-P6-06 | P6-performance.md | MEDIUM | PENDING | Redis provisioned but unused — no cache backend configured | - |
| A-P6-07 | P6-performance.md | MEDIUM | MEDIUM | Frontend no request deduplication / global loading state | - |
| A-P6-08 | P6-performance.md | MEDIUM | PENDING | LiveKit docker image uses :latest tag — no version pin | - |
| A-P7-08 | P7-oracle-deploy.md | MEDIUM | PENDING | No django check --deploy validation documented | - |
| A-P7-09 | P7-oracle-deploy.md | MEDIUM | PENDING | No environment-specific requirements split (psycopg2-binary in prod) | - |
| A-P7-10 | P7-oracle-deploy.md | MEDIUM | PENDING | No OCI networking documentation | - |
| A-P8-05 | P8-rbac-logic.md | MEDIUM | **FIXED** | SystemAccessViewSet.partial_update() hardened — owner self-deactivation blocked, self-demotion blocked, last-active-owner protection covers both is_active and role changes, role validated against USER_TYPES. | Y — verified via 69-passed run |
| A-P8-06 | P8-rbac-logic.md | MEDIUM | PENDING | Parent role sees wrong child if parent_account FK misassigned | - |
| **LOW — Audit** | | | | | |
| A-P1-12 | P1-security.md | LOW | PENDING | .env may be committed to git — check history | - |
| A-P2-09 | P2-data-backend.md | LOW | PENDING | Attendance unique_together conflicts with soft-delete | - |
| A-P3-09 | P3-api-contract.md | LOW | PENDING | TeacherListSerializer.get_classes() return type ambiguous (array vs int) | - |
| A-P4-08 | P4-incomplete.md | LOW | PENDING | RoleRoute.jsx dead 'student' fallback when user_type is null | - |
| A-P4-09 | P4-incomplete.md | LOW | PENDING | Recovery vault UI — restore action missing from AdminHub | - |
| A-P5-06 | P5-ux-a11y.md | LOW | PENDING | Client-side status filter slow on large datasets | - |
| A-P5-07 | P5-ux-a11y.md | LOW | PENDING | Mobile layout unverified for Classroom.jsx | - |
| A-P5-08 | P5-ux-a11y.md | LOW | PENDING | Settings.jsx console.error logs raw error with stack trace | - |
| A-P6-09 | P6-performance.md | LOW | PENDING | No Vite bundle splitting / code analysis | - |
| A-P6-10 | P6-performance.md | LOW | PENDING | No CDN / static file caching headers | - |
| A-P7-11 | P7-oracle-deploy.md | LOW | PENDING | docker-compose.yml version field deprecated | - |
| A-P8-07 | P8-rbac-logic.md | LOW | PENDING | is_active=False not enforced during JWT refresh | - |
| **LAUNCH-BLOCKING — Roadmap** | | | | | |
| R-R1-01 | R1-domain-gaps.md | LAUNCH-BLOCKING | DECISION NEEDED | ADD: Quran curriculum / Hifz progress tracker model — product feature | - |
| R-R1-02 | R1-domain-gaps.md | LAUNCH-BLOCKING | DECISION NEEDED | ADD: Structured class scheduling (replace free-text class_timing) — product feature | - |
| R-R1-03 | R1-domain-gaps.md | LAUNCH-BLOCKING | DECISION NEEDED | ADD: Server-side email/SMS notification delivery — product feature | - |
| R-R2-01 | R2-remove.md | LAUNCH-BLOCKING | **FIXED** | Cross-filed with A-P4-02 — fixed there (fake OPENAI_API_KEY removed from .env; ai_bot compose service commented out) | Y |
| R-R2-02 | R2-remove.md | LAUNCH-BLOCKING | **FIXED** | Deleted 3 dead `.todo` scaffolding files (`config/celery_app.py.todo`, `config/s3_storage.py.todo`, `config/sentry.py.todo`) — each contained only a single comment line, never executed by Python; added `*.todo` pattern to root `.gitignore` so they cannot reappear | Y — `Test-Path` confirms all 3 absent; `manage.py check` 0 issues |
| R-R2-03 | R2-remove.md | LAUNCH-BLOCKING | **FIXED** | Replaced hardcoded `username='quranific'` + `password='BROTHERfaisal.edu,123'` + `create_superuser` auto-create footgun with a safe CLI-arg script: requires `python reset_pw.py <username> <new_password>`; fails fast (before Django setup) with usage message if args missing or password <10 chars; update-only (no auto-create); docstring explains OCI usage and security notes | Y — no-args run exits 1 with usage text; PowerShell scan confirms `create_superuser`, hardcoded username/password not present as executable code |
| R-R2-04 | R2-remove.md | LAUNCH-BLOCKING | **FIXED** | Deleted `backend/node_modules/` (~30 MB of frontend LiveKit JS packages), `backend/package.json`, and `backend/package-lock.json` — none referenced by Django; Python LiveKit SDK already in `requirements.txt`; no local `.git` existed so no git-untrack step needed; added `node_modules/` to new root `.gitignore` so it cannot reappear when GitHub is connected | Y — `Test-Path` confirms all 3 absent; `manage.py check` 0 issues |
| R-R3-01 | R3-scalability.md | LAUNCH-BLOCKING | PENDING | ADD: Celery + Redis queue for async ops (cross-files A-P4-04) | - |
| R-R3-02 | R3-scalability.md | LAUNCH-BLOCKING | **FIXED** | Cross-filed with A-P6-02 — fixed there | Y |
| R-R3-03 | R3-scalability.md | LAUNCH-BLOCKING | **FIXED** | Added `Idempotency-Key` header support to `PaymentListView.post()`. Client sends `Idempotency-Key: <uuid>` on POST; server stores it in new nullable `idempotency_key` field (migration `0004_payment_idempotency_key`, unique index). Replay fast-path: second call with same key returns 200 + original payment data + `replayed: True` — no new row created. **Race guard:** concurrent requests that both pass the pre-check before either commits will lose the INSERT to an `IntegrityError`; explicit `except IntegrityError` block (before `except Exception`) catches this, queries the winner's payment, and returns 200 + `replayed: True` instead of 500. `logger.warning` emitted on race recovery for observability. No key → unchanged behaviour (backward compatible). 2Checkout webhook path unaffected (uses `transaction_id`). Three regression tests: normal replay, absent-key path, and race-condition guard (mock bypasses pre-check to trigger the IntegrityError path directly). | Y — `43 passed in 127.41s`; all three idempotency tests green |
| R-R3-04 | R3-scalability.md | LAUNCH-BLOCKING | PENDING | IMPROVE: Redis wired as Django cache backend | - |
| R-R3-05 | R3-scalability.md | LAUNCH-BLOCKING | **FIXED** | Rewrote `livekit/livekit.yaml`: replaced `devkey:secret` with `CHANGE_ME_` placeholder pair + operator checklist; set `use_external_ip: true` (was `false` — breaks WebRTC ICE on OCI NAT); added TURN stub with Coturn step-by-step instructions; added production `logging:` block; updated `.env` LiveKit section to use `CHANGE_ME_` placeholders with generation instructions; added module-load startup guard in `live_session/views.py` that logs `CRITICAL` if any known-insecure value (`devkey`, `secret`, `CHANGE_ME_*`, empty) is detected at gunicorn startup | Y — `manage.py check` 0 issues; CRITICAL log fires correctly on startup with placeholder keys; `use_external_ip: true` confirmed in yaml |
| R-R5-01 | R5-platform-maturity.md | LAUNCH-BLOCKING | **FIXED** | Added `sentry-sdk[django]==2.66.0` to requirements.txt. Initialized in `config/settings.py` only when `not DEBUG and SENTRY_DSN` — app runs cleanly without a DSN (local dev / CI). Integrations: `DjangoIntegration` (auto-captures unhandled request exceptions) + `LoggingIntegration` (ERROR-level logs → Sentry events, INFO+ → breadcrumbs). `send_default_pii=False` for GDPR. `SENTRY_TRACES_SAMPLE_RATE=0.1` (10% performance tracing). Env vars added to `.env`: `SENTRY_DSN=` (blank), `SENTRY_TRACES_SAMPLE_RATE`, `SENTRY_RELEASE`, `DJANGO_ENVIRONMENT`. **Capture verified** with an in-memory `MemoryTransport` (no real DSN needed): `test_sentry_captures_exception_with_memory_transport` raises a `ValueError`, calls `sentry_sdk.capture_exception()`, flushes, and asserts ≥1 envelope with an `event` item containing the exception payload. Test passes. `test_sentry_not_initialised_without_dsn` confirms no active client when DSN is blank. | Y — `53 passed in 129.19s`; both Sentry tests green |
| R-R5-02 | R5-platform-maturity.md | LAUNCH-BLOCKING | **FIXED** | **RequestID middleware** (`config/middleware.py`): `RequestIDMiddleware` assigns UUID4 per request (honours upstream `X-Request-ID` header if present), stores ID on `request.request_id` and in thread-local, echoes it as `X-Request-ID` response header. **try/finally cleanup guarantee:** `get_response()` is wrapped in `try/finally` so `_local.request_id = None` runs even when a view raises an unhandled exception — without this, a crash would leave the old ID in the thread-local, polluting the next request's log lines on a reused gunicorn thread. `RequestIDLogFilter` injects `request_id` into every log record (falls back to `"-"` outside a request). Added to `MIDDLEWARE` list after `SecurityMiddleware`. **LOGGING config** in `config/settings.py`: JSON formatter (`pythonjsonlogger.json.JsonFormatter` v4.x, using `fmt` + `rename_fields`), fields: `timestamp`, `level`, `logger`, `message`, `request_id`. Log levels: `django.*` → DEBUG in dev / WARNING in prod; `django.request` → ERROR (4xx/5xx only); all app loggers → INFO in both envs; root → WARNING. 11 tests in `test_sentry_logging.py`: JSON parse validity, request_id=`-` outside request, request_id from thread-local inside request, UUID4 assignment, upstream header honour, normal-path cleanup, **exception-path cleanup** (`crashing_view` raises RuntimeError → middleware `finally` still clears thread-local → subsequent normal request gets its own fresh ID), and API integration (X-Request-ID on response). | Y — `54 passed in 133.43s`; all 11 R-R5-02 tests green including exception-path |
| R-R5-03 | R5-platform-maturity.md | LAUNCH-BLOCKING | **WONTFIX** | No public registration surface exists. Both account-creation endpoints (`RegisterView`, `ParentRegistrationView` in `accounts/views.py`) require `[IsAuthenticated, IsOwnerOrManager]` — verified in code. All accounts are admin-created; there is no anonymous signup path for an attacker or user to exploit, so email-ownership verification has no applicable threat model in this app. | N/A |
| R-R5-04 | R5-platform-maturity.md | LAUNCH-BLOCKING | **FIXED** | Built minimum viable test suite: **40 tests, 4 files, all green** against real Postgres (pytest-django, factory-boy, Faker). Files: `tests/test_auth.py` (9 tests — login, token refresh, logout, unauth access, role-override regression), `tests/test_rbac.py` (17 tests — student destroy IDOR, update-credentials gate, teacher serializer scoping, payment POST permissions, system-access gate), `tests/test_payments.py` (7 tests — valid create, duplicate 409 A-P2-04 regression, different-months allowed, paginated GET A-P1-08 regression, parent data isolation, teacher/student blocked), `tests/test_attendance.py` (7 tests — teacher/owner/manager mark A-P1-07, student/parent blocked, duplicate date 400/409, data isolation). Infrastructure: `conftest.py` (db-fixture pattern, autouse throttle-disable), `factories.py` (6 role user factories, StudentFactory, PaymentFactory, AttendanceFactory), `pytest.ini`, `requirements-test.txt`. CI pipeline upgraded to real pytest run with Postgres service. Dev port-binding via `docker-compose.override.yml` (gitignored). | Y — `40 passed in 118.37s (0:01:58)` confirmed |
| **FAST-FOLLOW — Roadmap** | | | | | |
| R-R1-04 | R1-domain-gaps.md | FAST-FOLLOW | DECISION NEEDED | ADD: Admissions / trial-class public intake form — product feature | - |
| R-R1-05 | R1-domain-gaps.md | FAST-FOLLOW | DECISION NEEDED | ADD: In-platform messaging teacher↔student↔guardian — product feature | - |
| R-R1-06 | R1-domain-gaps.md | FAST-FOLLOW | DECISION NEEDED | ADD: Certificate / progress report PDF generation — product feature | - |
| R-R1-07 | R1-domain-gaps.md | FAST-FOLLOW | DECISION NEEDED | ADD: Multi-currency + invoice PDF — product feature | - |
| R-R1-08 | R1-domain-gaps.md | FAST-FOLLOW | DECISION NEEDED | IMPROVE: Parent portal — add assignments, grades, teacher contact | - |
| R-R1-09 | R1-domain-gaps.md | FAST-FOLLOW | PENDING | IMPROVE: Assignment grade — standardized scale (not free-text CharField) | - |
| R-R1-10 | R1-domain-gaps.md | FAST-FOLLOW | DECISION NEEDED | IMPROVE: RTL / Arabic UI support | - |
| R-R2-05 | R2-remove.md | FAST-FOLLOW | PENDING | REMOVE: RecoveryVault.jsx dead page (replaced by AdminHub tab) | - |
| R-R2-06 | R2-remove.md | FAST-FOLLOW | PENDING | REMOVE: /all-students duplicate route | - |
| R-R3-06 | R3-scalability.md | FAST-FOLLOW | PENDING | IMPROVE: SessionLog.participant_count race — use F() expression | - |
| R-R3-07 | R3-scalability.md | FAST-FOLLOW | PENDING | IMPROVE: PaymentListView — add DRF pagination (bypasses global) | - |
| R-R3-08 | R3-scalability.md | FAST-FOLLOW | PENDING | IMPROVE: Composite DB indexes (Student.assigned_teacher, SystemLog.timestamp) | - |
| R-R3-09 | R3-scalability.md | FAST-FOLLOW | PENDING | IMPROVE: Session backend — verify stateless or set Redis sessions | - |
| R-R4-01 | R4-edge-performance.md | FAST-FOLLOW | PENDING | ADD: CDN for static assets | - |
| R-R4-02 | R4-edge-performance.md | FAST-FOLLOW | PENDING | ADD: LiveKit edge multi-region / TURN server | - |
| R-R4-03 | R4-edge-performance.md | FAST-FOLLOW | PENDING | IMPROVE: Split StudentSerializer list vs detail (cross-files A-P2-01) | - |
| R-R4-04 | R4-edge-performance.md | FAST-FOLLOW | PENDING | IMPROVE: SystemLog — add server-side pagination + date/user filter | - |
| R-R4-05 | R4-edge-performance.md | FAST-FOLLOW | PENDING | IMPROVE: Frontend — React Query/SWR for deduplication | - |
| R-R5-05 | R5-platform-maturity.md | FAST-FOLLOW | PENDING | ADD: Password reset / forgot password flow | - |
| R-R5-06 | R5-platform-maturity.md | FAST-FOLLOW | PENDING | ADD: Admin login-as-user with audit trail (django-hijack) | - |
| R-R5-07 | R5-platform-maturity.md | FAST-FOLLOW | PENDING | IMPROVE: 2FA for owner/head_manager (django-otp) | - |
| R-R5-08 | R5-platform-maturity.md | FAST-FOLLOW | PENDING | IMPROVE: PWA — manifest.json + service worker | - |
| R-R5-09 | R5-platform-maturity.md | FAST-FOLLOW | PENDING | IMPROVE: API versioning — add /api/v1/ prefix | - |
| R-R5-10 | R5-platform-maturity.md | FAST-FOLLOW | PENDING | IMPROVE: Tailwind design system — extract shared Button/Card/Modal components | - |
| **LATER — Roadmap** | | | | | |
| R-R1-11 | R1-domain-gaps.md | LATER | DECISION NEEDED | ADD: Class session recording (LiveKit egress → S3 → HLS) | - |
| R-R1-12 | R1-domain-gaps.md | LATER | DECISION NEEDED | ADD: Gamification — streaks, badges, leaderboard | - |
| R-R3-10 | R3-scalability.md | LATER | DECISION NEEDED | ADD: Django Channels WebSocket layer for real-time push | - |
| R-R3-11 | R3-scalability.md | LATER | PENDING | ADD: Zero-downtime migration strategy | - |

---

## Notes

- Items marked `DECISION NEEDED` are product/architecture features that cannot be implemented without explicit user direction — they are skipped in the fix loop until the user explicitly assigns them.
- Cross-filed items (e.g. A-P6-01 same as A-P2-01) are fixed once; the duplicate ID is marked FIXED with a reference to the primary fix.
- `A-P8-01` is identical to `A-P1-01` (Login.jsx role override) — **FIXED** via A-P1-01.
